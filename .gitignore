__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
.DS_Store

# generated by fixture runs — rebuildable, never committed
evals/fixtures/*/export/

# Run records this repo generates when auditing itself. Local by design —
# engines/telemetry.md: records never leave the machine without an explicit act.
.vigil/
